function mostrar(){

    opc= parseInt(prompt("Ingresa opcion 1 para el area del rectangulo, 2 para el cuadrado, 3 "))
    switch(opc){
    
        case 1: 
        y= parseInt(prompt("Ingresa la base"))
        e= parseInt(prompt("Ingrese la altura"))
        var resultado= y*e;
        if (y!=0 && e!=0){
            document.write("El area del rectangulo es :" )
        }else{
            document.write ("Verifica tus datos de la base y altura")
        }
    break;
case 2:
    }
}
